클라이언트가 컨트롤러를 호출하기 위해선 공통 로직이 필요하다
이를 프론트 컨트롤러(Servlet)을 도입하면 공통 로직을 하나로 묶어서 클라이언트가 접근할 수 있다
이후 프론트 컨트롤러가 요청에 알맞은 컨트롤러를 호출한다
최종적으로 프론트 컨트롤러를 제외한 나머지 컨트롤러는 Servlet 을 사용하지 않아도 된다는 장점이 있다

**스프링 웹 MVC 의 DispatcherServlet 이 FrontController 패턴으로 구현되어 있음